#ifndef MAP_SELECTOR_H
#define MAP_SELECTOR_H

// Retorna o caminho completo do mapa escolhido ou NULL se cancelado.
// A string retornada é alocada dinamicamente (use free() após o uso).
char *SelecionarMapaPersonalizado(void);

#endif
